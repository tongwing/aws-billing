# Docker Compose Environment Variables
# Copy this file to .env in the root directory to use with docker-compose

# Port configuration
BACKEND_PORT=8000
FRONTEND_PORT=3000

# Frontend base path configuration
# For root path deployment (default):
REACT_APP_BASE_PATH=
# For sub-path deployment (example):
# REACT_APP_BASE_PATH=/aws-billing